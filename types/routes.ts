export enum Route {
  dashboard = "/dashboard",
  login = "/sign-in",
  register = "/sign-up",
}

export enum ApiRoutePatterns {
  login = "/api/auth/login",
  register = "/api/users",
  revalidate = "/api/auth/revalidate",
}

export enum RoutePattern {}
